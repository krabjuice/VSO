import React, { Component } from 'react'
import Logo from '../Logo';
import SubmitionForm from './SubmitionForm';

export default class Submitions extends Component {
    render() {
        return (
            <div>
                <Logo />
                <SubmitionForm />
            </div>
        )
    }
}
